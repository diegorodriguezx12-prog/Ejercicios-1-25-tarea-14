int total = 4;
for (int fila = 1; fila <= total; fila++)
{
    for (int columna = 1; columna <= total; columna++)
    {
        Console.Write($"({fila},{columna})  ");
    }
    Console.WriteLine();
}